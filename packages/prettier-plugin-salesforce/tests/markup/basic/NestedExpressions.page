<apex:page><apex:form><apex:pageBlock title="{!Account.Name}"><apex:pageBlockSection><apex:outputPanel rendered="{!NOT(ISBLANK(Account.OwnerId))}"><apex:outputText>{!IF(Account.IsActive__c, Account.Owner.Name, Account.CreatedBy.Name)}</apex:outputText></apex:outputPanel><apex:outputPanel rendered="{!  OR( Account.IsActive__c , NOT(ISBLANK(Account.OwnerId)) )  }"><apex:outputText>{!  IF( Account.IsActive__c , Account.Owner.Name , Account.CreatedBy.Name )  }</apex:outputText></apex:outputPanel></apex:pageBlockSection></apex:pageBlock></apex:form></apex:page>
